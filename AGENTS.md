# SnowyArk 开发约定

## 目录

- [工作入口](#工作入口)
- [代码约定](#代码约定)
- [C++ 项目基准](#c-项目基准)
- [验证](#验证)
- [文档与记录](#文档与记录)

## 工作入口

本文件适用于整个仓库, 仅补充项目专属约定. 通用行为, 语言, 编码和会话记录要求遵循全局规则; C++, 文档与 Markdown 要求分别遵循按需加载的共享 `cpp.md`, `doc.md` 和 `markdown.md`. 按任务阅读以下项目文档.

| 任务涉及的内容 | 必读文档 |
| --- | --- |
| 项目进度与已有功能 | [当前状态](README.md#当前状态) |
| 目录, 模块职责或依赖关系 | [工程结构](Engine/Docs/Architecture.md) |
| 编译, 测试, C++ 格式化或 Rider 配置 | [构建与运行](Engine/Docs/Building.md) |
| 源文件收集, 平台选择或编译选项 | [CMake 维护](Engine/Docs/Building.md#cmake-维护) |

项目功能状态以现有实现为准, 架构文档中的后续规划不表示已经完成.

## 代码约定

- 目录和 C++ 文件采用 PascalCase, `CMakeLists.txt` 等工具约定的文件名保持原样.
- 引擎类型及必要的非成员函数放在唯一命名空间 `SnowyArk` 中, 不再按 Runtime / Editor 拆嵌套命名空间. 项目自有 C++ 代码禁止使用匿名命名空间; 类专属辅助类型和常量归入类的私有成员, 函数归属遵循下节.
- 项目头文件统一使用 `#pragma once`.
- 项目 include 使用以 `Engine/Source/` 为根的 `<Runtime/...>`, `<Editor/...>` 或 `<Tools/...>` 路径, 不使用父目录相对路径; 详见 [依赖规则](Engine/Docs/Architecture.md#依赖规则).
- 每批自有 C++ 修改完成后, 对本次新增或修改的文件运行 clang-format, 使用仓库根目录 `.clang-format`, 并检查格式化差异. 不格式化第三方代码, 生成文件或无关文件. 工具版本, 命令和已知限制见 [C++ 格式化](Engine/Docs/Building.md#c-格式化).

## C++ 项目基准

以下条款用于后续新增或修改代码, 不表示现有实现已经全部符合.

- 语言基线为 C++20. 不引入要求 C++23 或更高版本的设施, 除非任务明确包含语言与工具链升级.
- 函数优先归入职责所属类. 类内部辅助函数设为 private static; 需要对象状态时使用实例成员. 已有共享工具类可以复用, 不把无关功能集中到一个 Utils 类中. 全局 `main`, 必需的非成员运算符, ADL 定制点及外部接口要求的函数保留非成员形式, 不为成员化破坏语言或 API 契约.
- GAL 的公开接口使用引擎类型. Vulkan 类型, 格式转换和底层句柄操作限于 Vulkan 后端; 工厂选择后端的既有边界见 [依赖规则](Engine/Docs/Architecture.md#依赖规则).
- 帧级借用接口需要说明跨帧, swapchain 重建和关闭时的有效期限制.
- GPU 资源同时满足 CPU 所有权和 GPU 使用期限要求. RAII 不能代替 fence, semaphore 等同步; 销毁, 重用或重建资源前, 必须确认相关 GPU 使用已结束, 或进入有明确定义的设备故障清理路径.
- 跳过本帧, 请求重建等正常可恢复状态使用返回值表达; 异常故障通过异常传播至应用边界处理.
- 关闭路径等待 GPU 失败后, 仍按既定策略尽力诊断并完成不抛异常的清理. Vulkan / GLFW 回调保留 API 要求的签名和调用约定.
- 每帧路径新增动态分配, GPU 等待或资源重建时, 在变更说明中交代必要性. 不把全设备等待当作常规帧同步方案; 性能优化以测量为依据, 不为预期收益引入额外架构.

## 验证

构建与测试命令见 [编译与测试](Engine/Docs/Building.md#编译与测试).

- 交付前, 对本次新增或修改的自有 C++ 文件执行 clang-format `--dry-run --Werror` 检查. 完成格式化后, 构建受影响目标并运行相关测试. 修改公共头文件或构建配置后, 检查两套构建配置.
- 修改源文件收集逻辑或更换生成器时, 实测新增, 重命名和删除文件后的直接构建, 检查 CMake 是否能自动更新配置. 验证结束后清理本次创建的探针文件, 再检查构建清单.
- 只修改文档时不必重新编译.

## 文档与记录

维护时必须遵守以下内容范围:

| 位置 | 面向读者与内容范围 | 不应写入的内容 |
| --- | --- | --- |
| 根 `README.md` | 面向使用者, 介绍项目用途, 当前状态, 简短使用入口, 文档链接和许可证 | 详细技术设计, 完整操作教程, AI 执行规则 |
| 根 `AGENTS.md` | 面向 AI, 规定工作流程, 代码约定, 验证要求, 文档维护规则和必读入口 | 模块设计正文, 功能手册, 构建细节, 项目进度的重复描述 |
| `Engine/Docs/` | 面向使用者和开发者, 保存架构, 模块与接口说明, 构建和使用指南, 设计决策; 其中 `README.md` 作为文档库索引 | AI 专用执行规则, 临时任务进度, 调试日志和会话记录 |

项目事实和技术细则只在所属主题文档中维护一份完整说明, 其他位置保留必要摘要和链接. 发现内容越界时, 将其移到对应位置并修正引用, 不在多处保留完整副本. 各主题入口见 [文档目录](Engine/Docs/README.md).

- 新增, 修改或移除功能, 以及调整构建配置, 目录, 依赖或开发约定时, 在同一次任务中完成相关文档更新. 功能进度变化时, 同步更新根 README 的当前状态. 文档尚未同步时, 不得将该项开发标记为完成, 也不得留待后续任务补写.
- 创建或修改中文文档时, 必须先读取并使用 `humanizer-zh-next` 技能润色. 本机技能文件为 `~/.agents/skills/humanizer-zh-next/SKILL.md`.
- 新增项目文档时, 同步更新文档库索引和相关链接. 已实现功能与后续规划分开写.
