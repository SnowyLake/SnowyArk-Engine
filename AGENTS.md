# SnowyArk 开发约定

## 目录

- [工作入口](#工作入口)
- [代码约定](#代码约定)
- [验证](#验证)
- [文档与记录](#文档与记录)

## 工作入口

本文件适用于整个仓库, 供 AI 开发时查阅. 修改前先检查相关源码和未提交的改动, 按任务阅读以下文档并遵循其中的约定.

| 任务涉及的内容 | 必读文档 |
| --- | --- |
| 项目进度与已有功能 | [当前状态](README.md#当前状态) |
| 目录, 模块职责或依赖关系 | [工程结构](Engine/Docs/Architecture.md) |
| 编译, 测试或 Rider 配置 | [构建与运行](Engine/Docs/Building.md) |
| 源文件收集, 平台选择或编译选项 | [CMake 维护](Engine/Docs/Building.md#cmake-维护) |

先核对现有实现, 再按当前任务修改. 架构规划不能作为功能已经完成的依据, 尚未需要的模块和抽象留到实际使用时再实现.

## 代码约定

- 目录和 C++ 文件采用 PascalCase, `CMakeLists.txt` 等工具约定的文件名保持原样.
- 命名空间, 类型和函数使用 PascalCase, 局部变量与函数参数使用 camelCase. 成员命名和代码布局遵循现有文件及已加载的 C++ 规则.
- 头文件使用 `#pragma once`. 新增或修改函数时, 用一句英文 Doxygen 注释说明用途; 声明和定义分开时, 注释放在声明处.
- 对话和文档使用中文文字与英文标点. 代码及代码注释使用英文.

## 验证

根据改动范围选择验证方式, 并说明实际执行的检查和未覆盖的部分. 构建与测试命令见 [编译与测试](Engine/Docs/Building.md#编译与测试).

- 修改 C++ 后, 构建受影响目标并运行相关测试. 修改公共头文件或构建配置后, 检查两套构建配置.
- 修改源文件收集逻辑或更换生成器时, 实测新增, 重命名和删除文件后的直接构建, 检查 CMake 是否能自动更新配置. 验证结束后清理本次创建的探针文件, 再检查构建清单.
- 只修改文档时, 检查事实, 本地链接, 目录和命令是否与仓库一致, 不必重新编译.

## 文档与记录

维护时必须遵守以下内容范围:

| 位置 | 面向读者与内容范围 | 不应写入的内容 |
| --- | --- | --- |
| 根 `README.md` | 面向使用者, 介绍项目用途, 当前状态, 简短使用入口, 文档链接和许可证 | 详细技术设计, 完整操作教程, AI 执行规则 |
| 根 `AGENTS.md` | 面向 AI, 规定工作流程, 代码约定, 验证要求, 文档维护规则和必读入口 | 模块设计正文, 功能手册, 构建细节, 项目进度的重复描述 |
| `Engine/Docs/` | 面向使用者和开发者, 保存架构, 模块与接口说明, 构建和使用指南, 设计决策; 其中 `README.md` 作为文档库索引 | AI 专用执行规则, 临时任务进度, 调试日志和会话记录 |

项目事实和技术细则只在所属主题文档中维护一份完整说明, 其他位置保留必要摘要和链接. 发现内容越界时, 将其移到对应位置并修正引用, 不在多处保留完整副本. 各主题入口见 [文档目录](Engine/Docs/README.md).

- 功能开发与文档维护必须同步进行. 开发前确定受影响的文档, 实现过程中同步更新功能行为, 接口, 用法和限制说明, 交付前核对文档与实现是否一致.
- 新增, 修改或移除功能, 以及调整构建配置, 目录, 依赖或开发约定时, 在同一次任务中完成相关文档更新. 功能进度变化时, 同步更新根 README 的当前状态. 文档尚未同步时, 不得将该项开发标记为完成, 也不得留待后续任务补写.
- 创建或修改中文文档时, 必须先读取并使用 `humanizer-zh-next` 技能润色. 本机技能文件为 `~/.agents/skills/humanizer-zh-next/SKILL.md`. 按文档读者和用途组织表达, 保留技术术语, 命令, 限制条件和来源, 不补写未经验证的功能或效果.
- 新增文档时, 同步更新文档库索引和相关链接. 保持文档内容完整, 结构清晰, 每篇 Markdown 文档保留标题层级和目录. 已实现功能与后续规划分开写.
- 临时脚本和工作记录放在 `.agent-sessions/{YYYYMMDD}-{short-kebab-case-session-summary}/`.
